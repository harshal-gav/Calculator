export const metadata = {
    title: "HTML Entity Encoder & Decoder - Escape & Revert HTML Tags",
    description: "Safely encode HTML tags into entities (&amp;lt; &amp;gt; &amp;amp;) to prevent execution, or automatically decode rendered entities back into readable source text.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
    return children;
}
