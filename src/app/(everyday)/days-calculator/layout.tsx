export const metadata = {
  title: "Days Calculator - Accurately Add & Subtract Dates",
  description:
    "Calculate exactly what day it will be after mathematically adding or subtracting years, months, weeks, and days from a given date. Handles leap years automatically. Including specialized USA tools like days calculator.",
  keywords: ["days calculator"],
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
